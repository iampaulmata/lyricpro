// Supabase SDK has been removed from the project. Keep a small
// stub so existing imports that check for `supabaseClient` continue
// to work and return `null` when the service is not configured.

// If you want to re-enable Supabase later, restore the original
// implementation and re-add `@supabase/supabase-js` to dependencies.

export const supabaseClient: null = null
